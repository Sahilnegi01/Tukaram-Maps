import {useState} from 'react';
const actions=['','SEALED','FINED','RAIDED','NOTICE_ISSUED','LICENSE_CANCELLED','SHUT_DOWN','DEMOLISHED'];
export default function Filters({value,onChange,onReset}){const [open,setOpen]=useState(false);const set=(k,v)=>onChange({...value,[k]:v});return <><button className="filterToggle" onClick={()=>setOpen(!open)} aria-expanded={open}>☷ Filters</button><section className={`filterBar ${open?'open':''}`} aria-label="Map filters"><div className="filterGroup typeGroup"><label>Establishment Type</label><div className="segmented"><button className={!value.type?'active':''} onClick={()=>set('type','')}>All</button><button className={value.type==='RESTAURANT'?'active':''} onClick={()=>set('type','RESTAURANT')}>Restaurants</button><button className={value.type==='HOTEL'?'active':''} onClick={()=>set('type','HOTEL')}>Hotels</button></div></div><label>Historical Action<select value={value.action_type} onChange={e=>set('action_type',e.target.value)}>{actions.map(x=><option key={x} value={x}>{x?x.replaceAll('_',' '):'All actions'}</option>)}</select></label><label>Current Status<select value={value.current_status} onChange={e=>set('current_status',e.target.value)}><option value="">All statuses</option><option value="OPEN">Open</option><option value="CLOSED">Closed</option><option value="UNKNOWN">Unknown</option></select></label><label>State / UT<input value={value.state} onChange={e=>set('state',e.target.value)} placeholder="All states"/></label><label>City<input value={value.city} onChange={e=>set('city',e.target.value)} placeholder="All cities"/></label><label className="dateFilter">Date Range<span className="datePair"><input aria-label="From date" type="date" value={value.from_date} onChange={e=>set('from_date',e.target.value)}/><input aria-label="To date" type="date" value={value.to_date} onChange={e=>set('to_date',e.target.value)}/></span></label><button className="reset" onClick={onReset}>Reset</button><button className="apply" onClick={()=>setOpen(false)}>Apply Filters</button></section></>}
